extends layout

block main
  h1= title
  form(method='get') 
    input(type="text" name="search" value=search)
    input(type="submit" value="szukaj")
  each item in data 
    article 
      h1.title=item.title
      h5.date=item.created 
      p.description=item.description
